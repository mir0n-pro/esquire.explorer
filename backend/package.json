{
  "name": "@mir0n-pro/esquire.backend",
  "version": "1.2.8",
  "description": "Esquire Frameworks Backend-for-Frontend (BFF) tier. Sits between explorer/frontend and the Spring Cloud Gateway; holds OIDC tokens server-side, serves the Angular SPA in production, and caches static entity dictionaries.",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-session": "^1.18.1",
    "openid-client": "^5.7.1",
    "http-proxy-middleware": "^3.0.3",
    "lru-cache": "^11.1.0",
    "pino": "^9.5.0",
    "pino-http": "^10.3.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/express-session": "^1.18.1",
    "@types/node": "^22.9.0",
    "@types/supertest": "^6.0.2",
    "supertest": "^7.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3",
    "vitest": "^2.1.5"
  }
}
